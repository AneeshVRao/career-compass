# Status / next steps

Living punch list — update this as items get done rather than treating it as a one-time snapshot.

1. ~~**Apply the multi-user auth migration, then backfill, then lock down.**~~ **Done**, against the live project (`iqqkvnjwrgyiigafxsqp`). Took two attempts on the main migration — the Supabase SQL editor runs a pasted script as one transaction, so a mid-script error (first a mangled `$$` from a copy/paste artifact, then an `anon`→`ano` typo on the `revoke`) rolled back everything else in that same run, including the policy swap. Confirmed via `pg_policies` that the old `USING (true)` policies were still active before the clean re-run. Backfill and lockdown then went cleanly: `ownerless_events`/`ownerless_settings` both hit 0 before the `NOT NULL` migration ran, and `/list` shows the historical events again under the real account.
2. **Enable Google OAuth in the Supabase dashboard.** The "Sign in with Google" button on `/login` returns a provider-not-enabled error until this is done (email/password works regardless). Needs a Google Cloud OAuth client, the provider toggled on in Supabase, and both localhost + Render `/auth/callback` URLs allowlisted. Runbook: `docs/DEPLOYMENT.md` → "Google OAuth setup runbook". Decide the "Confirm email" setting at the same time — recommended **on**, since signup is open.
3. ~~**Visually verify the redesign, now including `/login`.**~~ **Done.** All six pages were rendered and screenshotted at 1440×900 and 390×844, signed in against the live project with eight seeded events covering every status. The "dossier" direction holds up as built — navy ground, parchment admit-card stubs with the perforation and rotated type code, brass/stamp/ledger accents all read as intended. Four real problems were found and fixed:
   - **`/login` never said what the product was.** It was a bare centred card, and it is the first thing a new user sees. Now a two-column layout: a headline plus one sentence of what the app does on the left, the credential form on the right, and a **specimen entry** rendered with the real `EventCard` component (fed a fixed literal row) so the signature element previews itself and can never drift from the component. A rotated stamp-red "SPECIMEN" overprint was tried and **cut** — it overlapped the card corner and read as a rendering mistake rather than a stamp; a quiet mono caption does the same job.
   - **The sidebar jumped between pages.** `AppShell` renders "New entry" only when a page passes `onNew`, and `/settings` was the one page that didn't — so every nav item shifted up by the button's height when you navigated there. Settings now mounts the same shared `EventDrawer` the other four pages use, so the button is real rather than reserved space.
   - **The dashboard chart tooltip read `value : 1`** — Recharts' raw dataKey. The `Bar` now carries `name="Entries"`, so it reads "Interview R2 · Entries : 1". Verified by hovering the plot area, not just by reading the diff.
   - **`/login`'s heading was a second `<h1>`.** The left panel's headline is the page `h1` now and the form heading is an `h2`. Still matched by `getByRole("heading")`, so the specs were unaffected.

   Two things that looked like bugs in the screenshots and are **not**: the mobile bottom nav appears to overlap content in a full-page screenshot, but `AppShell` already ends with an `h-16 md:hidden` spacer — fixed elements just render at their viewport position in a stitched capture. And `/board`'s columns are cut off at the right edge, but the strip is `overflow-x-auto` and scrolls; ten `w-72` columns cannot fit any laptop. A gradient fade was considered and skipped as decoration the native scrollbar already covers.

4. ~~**Run the new E2E auth specs against a real project.**~~ **Done — 11 passed, 1 skipped, 0 failed.** The signup spec's failure was re-diagnosed and turned out to have **two** stacked causes, only one of which was the environmental quota this item originally recorded:

   - **A real defect, not environmental.** `uniqueTestEmail()` minted addresses `@example.com`, and Supabase's _public_ signup endpoint rejects that outright — `400 email_address_invalid`, because it is an RFC-2606 reserved domain. The spec never got as far as the quota; it failed on the address. This hid for so long because the **admin** API applies no such check, so every admin-created user worked fine and only the one spec driving the real form was affected. Fixed by minting from an unregistered non-reserved domain instead. Confirmed by probing `/auth/v1/signup` directly with three domains: `example.com` → 400 invalid, the other two → 429 rate limit, i.e. past validation.
   - **The quota is real, and now reported honestly.** With a valid address the endpoint answers `429 over_email_send_rate_limit`, since each run spends one email against the hourly cap. The spec now resolves to a _named_ outcome (`confirm` / `session` / `rate-limited`) and calls `test.skip()` on the quota case, so it reports as a skip with the reason instead of an opaque 20-second timeout on a boolean poll. A genuine regression still fails loudly.

   Converting it wholesale to the admin API — the fix this item originally suggested — was **rejected**: the provisioning spec already covers that path, and doing so would delete the only coverage of the real signup form.

   **Run these with `--workers=2`.** At Playwright's default (6 workers here) the specs contend for one Vite dev server and time out at the 30s default — `page.goto` waits past the limit while routes compile on demand, surfacing as `net::ERR_ABORTED` or "browserContext.close: Test ended". The same specs pass individually and at 2 workers. Nothing in the app is wrong; the dev server is the bottleneck. If this gets annoying, raise `timeout` in `playwright.config.ts` rather than chasing it as a flake.

5. ~~**`board.spec.ts` and `dashboard.spec.ts` still assume no auth.**~~ **Fixed.** Both now create a disposable confirmed user in `beforeEach` and sign in via `signIn()` before visiting the protected route, deleting the user in `afterEach` — same pattern as `auth.spec.ts`/`multi-user-isolation.spec.ts`. Still unrun here for the same reason as item 4 (no `.env`, migration not yet applied) — run alongside item 4's command once step 1 is done.
6. **Turn on GitHub branch protection for `main`.** The repo now has real history (bootstrapped with one direct push, see `docs/DECISIONS.md`) — enabling branch protection turns "every change gets a PR" from a convention into something GitHub actually enforces.
7. ~~**Sign-in silently did nothing before the page hydrated.**~~ **Fixed.** Five specs failed identically, stuck at `http://localhost:8080/login?`, with no error rendered. It looked exactly like a cookie/session bug — the guard bouncing a freshly signed-in user — and it was not: `signInWithPassword()` was never called at all. There was **no request to Supabase's `auth/v1` endpoint and no cookie ever set**. `/login` is SSR'd, so the form is on screen and clickable before React attaches `onSubmit`; a click in that window performs a **native** form submit, which reloads `/login?` — the empty query string being the tell, since no input carries a `name` (which is also why no credential ever reached the URL). Typing was lost the same way: it landed only in the DOM, and hydration then reset the controlled inputs to their empty `value` prop. A human never hit this because typing an email and password takes longer than hydration; Playwright fills and clicks in milliseconds. Fixed with one native attribute — a `<fieldset disabled>` around the form gates the inputs and submit button together, and a disabled default button also suppresses implicit Enter-key submission. The two controls outside the form (Google, and the sign-in/create-account toggle) got the same guard, since a pre-hydration click on those was silently a no-op — that is what stalled the signup spec on an unchanged heading. Playwright's actionability checks wait for enabled/editable, so the specs now block on hydration by themselves and needed no changes.
8. **`/auth/callback` has never once been executed.** Google OAuth is still off (item 2), so the entire OAuth branch is unexercised — and it is the one remaining place a cookie round-trip could fail the same way item 7 appeared to. `handleCallback()` calls `exchangeCodeForSession(code)`, which writes the session through `getSupabaseServerClient()`'s `setAll` → TanStack Start's `setCookie()`, and then returns a **hand-built** `new Response(null, { status: 302 })`. That only works if the framework merges the cookies recorded on the request context into a response the handler constructed itself. If it doesn't, OAuth sign-in sets no cookie and bounces straight back to `/login` — indistinguishable from the bug in item 7. Verify this the moment item 2 is done, and check for a real `Set-Cookie` on the 302 rather than trusting the redirect.
9. **Every navigation costs a round-trip to Supabase's auth server.** The root `beforeLoad` calls `fetchAuthUser()`, which calls `supabase.auth.getUser()` — a network call to `/auth/v1/user` that blocks the route resolving. Three fired for a single sign-in. `getClaims()` verifies the JWT locally against cached JWKS and would remove most of that latency, but it is a real change to how auth is validated (local signature check vs. asking the server), so it is a decision, not a cleanup. Related: if `getUser()` ever refreshes an expired token during SSR, the refreshed cookie is written via `setCookie()` after headers may already be flushed, and that write is silently lost — the same fragile class as item 8.
10. ~~**`EventCard` formats dates in local time inside an SSR'd component.**~~ **Fixed** by pinning a single display timezone — see `docs/DECISIONS.md` (2026-08-05) for why that beat client-only formatting. All date rendering now goes through `src/lib/datetime.ts` (`VITE_DISPLAY_TIME_ZONE`, default `Asia/Kolkata`), covered by 18 unit tests.

    Chasing this turned up **two bugs worse than the hydration flash it started as**, both live in production:
    - **Every reminder email stated the wrong time.** `run-reminders.ts` runs on Render, which is UTC, so `toLocaleString()` in the email body and `toLocaleTimeString()` in the subject rendered a 21:00 IST interview as "3:30 PM" — five and a half hours off, in the one output the user acts on. It survived because no test asserted the "When" row; one does now.
    - **`/calendar` bucketed events into the wrong day.** Day keys came from `format(new Date(e.start_at), "yyyy-MM-dd")`, which reads host-local fields, so any event after 18:30 UTC sat in the previous cell server-side. Bucketing now uses `zonedDayKey()`; the grid itself still uses date-fns, but seeded from `todayInZone()` so it stays deterministic.
    - Also fixed in passing: `EventDrawer` round-tripped its `datetime-local` inputs through `getTimezoneOffset()` — the _browser's_ offset — so editing an event from a machine in another zone silently rewrote its start time.

11. ~~**The `src/components/ui/` tree is 90% dead weight.**~~ **Done**, as its own PR so the diff stayed reviewable. **37 of the 46** shadcn components deleted along with `src/hooks/use-mobile.tsx` (whose only importer was the unused `sidebar.tsx`) — **3,920 lines**. The nine kept are the ones actually imported from outside the directory: `button`, `card`, `input`, `label`, `select`, `sheet`, `sonner`, `switch`, `textarea`. All nine were verified to import no other local `ui/*` file, so the dead set cut as one unit with nothing dangling.

    The counts differ slightly from the original audit note (46 files not 42, 37 dead not 38) — re-derived here rather than taken on trust. **33 dependencies** dropped with them, 57 → 25: 22 `@radix-ui/*` packages, `cmdk`, `embla-carousel-react`, `input-otp`, `react-day-picker`, `react-resizable-panels`, `vaul`, `react-hook-form`, `@hookform/resolvers`, `zod`, and `@dnd-kit/sortable` + `@dnd-kit/utilities` (only `@dnd-kit/core` is actually imported, by `board.tsx`). Lint warnings fell 7 → 2, since four of them lived in files that no longer exist.

    Three things the automated scan flagged that are **not** dead, and were kept: `react-dom` (referenced as a config string in `vite.config.ts`, and React's renderer), `@tanstack/router-plugin` (build tooling — a pinned dependency of `@tanstack/start-plugin-core`, which is what regenerates `routeTree.gen.ts`; nothing imports it from source by design), and every devDependency (all are CLI tools invoked by scripts, never imported). A dependency checker that only greps for import specifiers will report all of these as unused — they are the reason it can't be run unattended.

    Still open as a note: `ui/sonner.tsx` is a 23-line wrapper that only forwards props to `<Sonner>` with a `classNames` block, for a single call site in `__root.tsx`.

12. ~~**Widen coverage past three files.**~~ **Done.** `coverage.include` now also lists `src/routes/api/public/run-reminders.ts` and `src/lib/datetime.ts`, so the 80% threshold covers the code that sends real email. Reported 94.4% statements / 86.7% branches / 97.9% functions / 96% lines across the included set — comfortably over, with no test changes needed to get there.

13. ~~**Server functions had no CSRF protection.**~~ **Fixed.** Surfaced by TanStack Start's own startup warning during an E2E run, not by an audit. `createCsrfMiddleware` is now registered in `src/start.ts`, scoped to `handlerType === "serverFn"`.

    This was **not** closing a live hole, and it is worth being precise about why rather than overstating the fix: the only server function is `fetchAuthUser`, a read-only `GET` that mutates nothing, and the same-origin policy already stops a cross-site page from reading its response. The value is that the default is now safe — the first server function that _does_ mutate something is protected without anyone having to remember to think about it.

    Scoped to server functions rather than all requests on purpose: a blanket filter would also gate ordinary document navigations, and `/api/public/run-reminders` is called by `pg_cron` with no `Origin`, `Referer`, or `Sec-Fetch-Site` header at all — it authenticates with its own shared secret instead, and would start returning 403 to the cron. Verified with the full E2E suite (sign-in, board CRUD, and cross-user isolation all still pass).

## Audit follow-ups, deliberately deferred

Two findings from the 4-way audit of `feat/multi-user-auth` were considered and consciously **not** implemented. Recorded here so they read as decisions rather than oversights — reopen them if the premises below stop holding.

- **Rate limiting on `/login`** — not needed, and not actually implementable here. `/login` is a client-side page: it calls `supabase.auth.signInWithPassword()` / `signUp()` straight from the browser to Supabase's hosted Auth API. This app owns **no** endpoint in the credential path (`src/lib/auth-server.ts` only _reads_ the session cookie), so any limiter added to this codebase would sit in front of a page an attacker can simply skip — they would post directly to `{SUPABASE_URL}/auth/v1/token` with the anon key, which is public by design. Supabase's own server-side per-IP limits on the token/signup endpoints are therefore the only enforcement point that exists, and they are sufficient. If the threat model tightens, the fix is to **lower the limits in the Supabase dashboard** (Auth → Rate Limits) — not to add a rate-limiting dependency to this repo. Revisit only if a custom server-side sign-in route is ever introduced.
- **Structured logging (Pino/Winston/Sentry) instead of `console.error`** — flagged by two audits; deferred as disproportionate. This is a small tool that went from one user to a handful, running as a single Render web service where `console.error` already lands in the platform's log stream and is greppable there. A logging framework would add a dependency and a config surface to buy structure nobody is currently querying. `console.error` stays. Revisit when there is somewhere that actually consumes structured logs (log aggregation, alerting, or error grouping) — that need, not the log calls themselves, is the trigger.

~~Multi-user auth implementation~~ — done in code: cookie-based Supabase sessions, `/login` (email/password + Google), one server-side route guard, per-user RLS migrations, per-user reminder fan-out, sign-out in `AppShell`. Build, unit tests, and lint verified. The remaining work is all dashboard/SQL-editor configuration — items 1, 2 and 5 above.

~~Run the reminder cron migration in the Supabase SQL editor~~ — done. The job is scheduled and verified end-to-end. See `docs/DEPLOYMENT.md`.

~~Apply the initial schema migration~~ — done. The new Supabase project (`iqqkvnjwrgyiigafxsqp`) has its `events`/`settings` tables, enums, RLS, and extensions in place.

~~Supabase access review~~ — resolved by switching to a fresh, self-created Supabase project with no Lovable Cloud history. See `docs/DEPLOYMENT.md`.

~~Deploy to Render~~ — done. Live at `https://career-compass-nowy.onrender.com` (Blueprint ID `exs-d9h1f1brjlhs73dcb7ag`). `/`, `/board`, `/calendar` verified with 200s.

~~Set production env vars on Render~~ — done as part of the Blueprint deploy above.
